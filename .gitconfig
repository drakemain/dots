[user]
	email = drakemain@protonmail.com
	name = Drake Main
	signingkey = 
[commit]
	gpgsign = true
[core]
	editor = nvim
[diff]
	tool = vimdiff
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
